import SwiftUI

enum AppUpdateButtonColors {
    static let background = Color.ascendAccent
    static let foreground = Color.black

    static let backgroundSRGB = SIMD3<Double>(134 / 255, 211 / 255, 10 / 255)
    static let foregroundSRGB = SIMD3<Double>(repeating: 0)
}
