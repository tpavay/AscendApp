{
  "name": "functions",
  "scripts": {
    "lint": "eslint .",
    "build": "node -e \"require('fs').rmSync('lib', { recursive: true, force: true })\" && tsc",
    "test": "npm run build && node --test lib/test/*.test.js",
    "test:emulator": "npm run build && npx -y firebase-tools@15.22.1 emulators:exec --project demo-ascend-leaderboard-derivation --only firestore \"node --test lib/test/emulator/*.test.js\"",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "24"
  },
  "main": "lib/src/index.js",
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0",
    "globals": "^17.7.0",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.65.0"
  },
  "overrides": {
    "uuid": "^11.1.1"
  },
  "private": true
}
